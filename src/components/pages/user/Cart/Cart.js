import React from "react";

const Cart = () => {
  return (
    <div>
      User will add the product into cart which we can save into database or
      Local storage
    </div>
  );
};

export default Cart;
