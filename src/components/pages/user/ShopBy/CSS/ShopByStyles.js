import { makeStyles } from "@material-ui/core";

const ShopByStyles = makeStyles({
  shopByHeader: {
    textAlign: "center",
    padding: "16px 0px",
    fontWeight: 300,
    fontSize: "2rem",
  },
});

export { ShopByStyles };
