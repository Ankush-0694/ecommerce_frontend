 import { makeStyles } from '@mui/styles';

const ShopByStyles = makeStyles({
  shopByHeader: {
    textAlign: "center",
    padding: "16px 0px",
    fontWeight: 300,
    fontSize: "2rem",
  },
});

export { ShopByStyles };
